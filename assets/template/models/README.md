# Models

Create one folder directly inside this directory for each model:

```text
models/
  <model>/
```

A model folder has no required internal structure or written record. Keep selected and otherwise useful production files in whatever arrangement works with the tools being used.
